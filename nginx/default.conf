# Intentionally empty. This file replaces Alpine's default Nginx virtual host, which otherwise
# becomes the default server and returns 404 for every request. The application virtual host is
# defined in server.conf.
